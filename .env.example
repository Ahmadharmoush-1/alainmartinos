# Public site URL (used for canonical links, sitemap, Open Graph)
NEXT_PUBLIC_SITE_URL=https://salonalain.com
# Optional: endpoint that receives the contact form (e.g. Formspree, your CRM webhook).
# Leave empty and the form falls back to a pre-filled WhatsApp message.
NEXT_PUBLIC_FORM_ENDPOINT=
